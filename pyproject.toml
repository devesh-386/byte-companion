[project]
name = "companion"
version = "0.1.0"
description = "Fully local AI companion - no API keys"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
